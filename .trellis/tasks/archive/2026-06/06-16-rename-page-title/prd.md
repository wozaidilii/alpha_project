# Rename Page Title

## Goal

Replace the default Create T3 App browser title with the product name requested by the user.

## Requirements

- Change the site metadata title from `Create T3 App` to `HIstroGuessr`.
- Keep the change limited to browser/document metadata unless adjacent metadata is clearly the same scaffolded default.

## Acceptance Criteria

- [x] The root Next metadata title is `HIstroGuessr`.
- [x] No unrelated routes, gameplay logic, or styling are changed.
- [x] Typecheck or build validation passes.

## Definition Of Done

- Focused source change is committed.
- Relevant validation result is reported.

## Out Of Scope

- Rebranding visible in-app copy beyond the browser title.
- Changing favicon, logo, app icons, or route names.

## Technical Notes

- `src/app/layout.tsx` contains the default T3 scaffold metadata:
  - `title: "Create T3 App"`
  - `description: "Generated by create-t3-app"`
- The requested exact title string is `HIstroGuessr`.
